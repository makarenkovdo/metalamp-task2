mixin masked-text-field(text, name,placeholder)
    span.text-field-container
        input(class="masked-text-field__input js-masked-text-field__input"
            type = 'text'
            name = name
            placeholder = placeholder
            maxlength="10"
            ) 


